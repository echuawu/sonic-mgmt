{
    "packet_info": {
        "outer": {
            "{{packet.ip_type}}": {
                "dip": "{{packet.outer_dip}}",
                "sip": "{{packet.outer_sip}}",
                "proto": {{packet.outer_proto}}
            },
            "layer2": {
                "ethertype": 2048,
                "smac": "{{packet.outer_smac}}",
                "dmac": "{{packet.outer_dmac}}"
            },
            "tcp_udp": {
                "dport": {{packet.outer_dport}},
                "sport": {{packet.outer_sport}}
            },
            "vxlan_nvgre": {
                "vni": 4000
            }
        },
        "inner": {
            "{{packet.ip_type}}": {
                "dip": "{{packet.inner_dip}}",
                "sip": "{{packet.inner_sip}}",
                "proto": {{packet.inner_proto}}
            },
            "layer2": {
                "ethertype": 2048,
                "smac": "{{packet.inner_smac}}",
                "dmac": "{{packet.inner_dmac}}"
            },
            "tcp_udp": {
                "dport": {{packet.inner_dport}},
                "sport": {{packet.inner_dport}}
            }
        }
    }
}