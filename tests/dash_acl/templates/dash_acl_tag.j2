[
{% for prefix in prefix_list %}
{% if acl_tag == 'AclTagNegativeName' %}
{% set acl_tag_name = '' %}
{% else %}
{% set acl_tag_name = acl_tag + loop.index|string %}
{% endif %}
    {
        "DASH_PREFIX_TAG_TABLE:{{ acl_tag_name }}": {
            "ip_version": "{{ip_version}}",
            "prefix_list": "{{prefix}}"
        },
        "OP": "{{op}}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
]
